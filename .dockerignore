# Tudo é ignorado por padrão; só o necessário para o build é liberado abaixo.
*

!src/
!include/
!main.cpp
!Makefile

# Garante que lixo de build local nunca entre no contexto
**/*.o
**/*.d
**/*.gch
**/application
**/app
**/build/
